# CLAUDE.md

## Git

- Commit author for this repo is `Joy Fan <joy.fan.157@gmail.com>` (set via repo-local git config).
- Do NOT add a `Co-Authored-By` trailer to commit messages.
- Automatically `git commit` AND `git push` after completing a feature (durably
  authorized by Joy). Only commit complete, building, verified work — not
  half-finished or experimental changes. Always report the commit hash and a
  summary afterward.
